[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vamp-oauth-audit"
version = "1.1"
description = "OAuth 2.0 and OIDC security flow auditor"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "VampSecure Studios", email = "contact@vampsecurestudios.com" }]
keywords = ["security", "pentest", "audit", "cybersecurity", "vampsecure", "oauth", "oidc", "jwt", "authentication"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
]
dependencies = [
    "aiohttp>=3.9.0",
    "rich>=13.7.0",
]

[project.scripts]
vamp-oauth-audit = "vamp_oauth_audit:main"

[project.urls]
Homepage = "https://github.com/Vampsecure-Labs/vamp-oauth-audit"
Repository = "https://github.com/Vampsecure-Labs/vamp-oauth-audit"

[tool.setuptools]
py-modules = ["vamp_oauth_audit"]
